<% if (@host.puppet_aspect.try(:environment)) or @hostgroup %>
  <%= render 'puppetclasses/class_selection', :obj => @host %>
<% else %>
  <%= alert :class => 'alert-warning', :text =>  _('Please select an environment first') %>
<% end %>
